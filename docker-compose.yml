services:
  backend:
    build:
      context: .
      target: backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      target: frontend
    depends_on:
      - backend
    ports:
      - "3000:80"

